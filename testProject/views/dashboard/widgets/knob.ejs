<div class='col-lg-3 col-md-4 col-xs-6'>
    <div class='panel panel-default'>
        <div class="widget large-height" data-filter="<%= widget.data_filter %>" visualization="<%= widget.name %>" data-id="<%= widget.unique_id %>" data-options="<%= widget.options %>">
            <div class="knob-help">
                <i class="fa fa-question-circle"></i>
            </div>
            <div id="knob-title" class="row">
                <%= widget.display_name %>
            </div>
            <div class="row subtitle">
                <% if (widget.display_name_2 !== null) { %>
                    <%= widget.display_name_2 %> &#183; 
                <% } %>
                <span id="knob-timestamp">Connecting to server...</span>
            </div>
            <div id="knob-content-<%= widget.unique_id %>" class="content">
                <img id="loader-<%= widget.unique_id %>" style="position: relative; top: calc(50% - 32px + 15px);" src="/images/loading.gif">
                <div class="knob-header row">
                    <div class="knob-temperature collapse"></div>
                </div>
                <div class="row">
                    <div class="knob-graph" id="knob-graph-<%= widget.unique_id %>"></div>
                    <div class="knob-alarm br-lightred collapse"></div>
                </div>
                <div class="row">
                    <div class="knob-line-graph" id="knob-line-graph-<%= widget.unique_id %>">
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>